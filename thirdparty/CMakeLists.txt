include (zlib.cmake)
include (zstd.cmake)
include (libdwarf.cmake)
add_subdirectory (libiberty)
add_subdirectory (getoptW)
set(BUILD_SHARED_LIBS OFF)
set(USE_ZLIB OFF)
set(USE_LIBDWARF OFF)
add_subdirectory(dwarfstack)
